{% import '@RichIdCookiesRegulation/partials/_url_config.html.twig' as urlMacro %}

{
    header: '{{ config.header|trans|e('js') }}',
    relatedCompaniesCount: {{ config.related_companies_count }},
    relatedCompaniesPrivacyPolicyUrl: '{{ urlMacro.parse(config.related_companies_privacy_policy_url)|trim }}',
}
